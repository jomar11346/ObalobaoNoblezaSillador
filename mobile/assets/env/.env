APP_NAME=Yui Blooms
# Windows desktop / web (this machine):
API_BASE_URL=http://127.0.0.1:8000

# If your Laravel serves images from /storage (via `php artisan storage:link`),
# keep this same as API_BASE_URL unless you use a different host.
IMAGE_BASE_URL=http://127.0.0.1:8000
